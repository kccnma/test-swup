document.addEventListener("DOMContentLoaded", function() {
  // SWUP: Initial Animate in
  setTimeout(function() {
    document.documentElement.classList.remove("is-animating");
  }, 100);

  // SWUP: enable swup
  const swup = new Swup();
});
